let openContact = document.getElementById("contact-modal")

function contactModal() {
    openContact.classList.add('contact-active');
}

function closeContactModal() {
    openContact.classList.remove('contact-active');
}